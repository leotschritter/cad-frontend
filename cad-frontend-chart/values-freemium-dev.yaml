# Freemium Tier - Development Environment
namespaceOverride: freemium

# GCP Project Configuration
projectId: "iaas-476910"

# API Gateway Configuration
apiGateway:
  baseUrl: https://api-freemium.dev.tripico.fun

# Use development ingress
ingress:
  name: cad-frontend-freemium-ingress
  host: frontend-freemium.dev.tripico.fun
  sslRedirect: "true"
  forceSslRedirect: "true"
  issuerName: letsencrypt-prod
  secretName: frontend-freemium-tls

# Lower resources for freemium tier
cadFrontend:
  name: cad-frontend-freemium
  port: 80
  image: "ghcr.io/leotschritter/cad-frontend:latest"
  replicas: 1

  # Feature flags for freemium tier
  features:
    weather: true
    recommendations: false  # Disabled for freemium
    travelWarnings: true

  resources:
    requests:
      memory: "128Mi"
      cpu: "50m"
    limits:
      memory: "256Mi"
      cpu: "250m"

  probes:
    startup:
      path: /
      initialDelaySeconds: 5
      periodSeconds: 5
      failureThreshold: 10
      timeoutSeconds: 3
    readiness:
      path: /
      periodSeconds: 10
      initialDelaySeconds: 0
      failureThreshold: 3
      timeoutSeconds: 5
    liveness:
      path: /
      periodSeconds: 30
      initialDelaySeconds: 10
      failureThreshold: 3
      timeoutSeconds: 3

podAnnotations:
  tier: freemium
